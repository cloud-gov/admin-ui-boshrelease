# abort script on any command that exits with a non zero value
set -e -x

bundle_cmd=/var/vcap/packages/ruby/bin/bundle
mysqlclient_dir=/var/vcap/packages/mysqlclient
libpq_dir=/var/vcap/packages/libpq
source "${BOSH_PACKAGES_DIR:-/var/vcap/packages}/ruby-3.1/bosh/compile.env"

cp -a ${BOSH_COMPILE_TARGET}/{admin_ui,vcap-common} ${BOSH_INSTALL_TARGET}

cd ${BOSH_INSTALL_TARGET}/admin_ui
bosh_bundle config build.mysql2 
bosh_bundle config build.pg --with-pg-lib=$libpq_dir/lib --with-pg-include=$libpq_dir/include
bosh_bundle config build.sequel_pg --with-pg-lib=$libpq_dir/lib --with-pg-include=$libpq_dir/include
bosh_bundle config build.sqlite3 --with-sqlite3-dir=/var/vcap/packages/sqlite
bosh_bundle install --local --deployment --without=development test

cd ${BOSH_INSTALL_TARGET}/vcap-common
bosh_bundle install --local --deployment --without=development test
